mutation AddPayment($fromIban: String!, $toIban: String!, amount: Double!) {
  addPayment(payment: { fromIban: $fromIban, toIban: $toIban, amount: $amount }) {
    fromIban
    toIban
    amount
  }
}
